{
	"name": "@smithery/mcp-strava",
	"version": "1.0.0",
	"type": "module",
	"scripts": {
		"build": "wrangler deploy --dry-run --outdir=dist",
		"dev": "wrangler dev --port 4346",
		"deploy": "wrangler deploy --minify"
	},
	"dependencies": {
		"hono": "^4.11.7",
		"@modelcontextprotocol/sdk": "^1.25.1",
		"zod": "^3.24.0"
	},
	"devDependencies": {
		"wrangler": "^4.4.0"
	}
}
